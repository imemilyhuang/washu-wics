import React from 'react'

const Member = ({data}) => {
  return (
    <div>
        Shows edit buttons next to name, position, (something to move order around),
        picture (upload and delete)
    </div>
  )
}

export default Member